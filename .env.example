# Docker Compose - MySQL
MYSQL_ROOT_PASSWORD=change-me-rootpw
MYSQL_DATABASE=community
MYSQL_USER=asdfid
MYSQL_PASSWORD=change-me-userpw
MYSQL_PORT=3307

# Docker Compose - Redis
REDIS_PORT=6379

# Spring Boot - DB (application.properties에서 사용)
DB_USERNAME=asdfid
DB_PASSWORD=change-me-userpw

# Spring Boot - Mail (application.properties에서 사용)
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_gmail_app_password

# Spring Boot - Redis (application.properties에서 사용)
REDIS_HOST=localhost

# Spring Boot - Kafka
NOTIFICATION_KAFKA_ENABLED=true
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_CLUSTER_ID=your_cluster_id